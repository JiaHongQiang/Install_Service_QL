#===============================================================================
# 麒麟系统安装服务 - 配置文件
# Kylin OS Service Installation Configuration
#===============================================================================
# 说明：
# 1. 此文件同时包含黑区和红区的配置
# 2. 留空的参数将在安装时提示交互式输入
# 3. 以 # 开头的行为注释
#===============================================================================

#===============================================================================
#                              通用配置
#===============================================================================

# 数据库密码（留空则安装时提示输入）
DB_PASSWORD=""

# 域代码 (通用)
DOMAIN_CODE="2c4d54e8ba"

# 主节点IP地址 (通用)
MAIN_IP="192.168.2.36"

# 网关用户ID (通用)
GW_USER_ID="581212517"

#===============================================================================
#                              黑区配置 (BLACK ZONE)
#===============================================================================

[BLACK]

# 黑区节点ID
BLACK_NODE_ID="20"

# 黑区本地IP地址
BLACK_LOCAL_IP="192.168.2.156"

# 黑区加密类型: -1=未加密, 3=黑区加密
BLACK_ENCRYPT_TYPE="3"

#-------------------------------------------------------------------------------
# 黑区加密卡配置 (FPGA)
#-------------------------------------------------------------------------------

# 黑区加密卡代理IP
BLACK_FPGA_AGENT_IP="192.168.55.28"

# 协商端口 (默认: 16001)
BLACK_FPGA_NEGOTIATION_PORT="16001"

# 节点端口 (默认: 16002)
BLACK_FPGA_NODE_PORT="16002"

# 数据端口 (默认: 16003)
BLACK_FPGA_DATA_PORT="16003"

# 联系端口 (默认: 500)
BLACK_FPGA_CONTACT_PORT="500"

#-------------------------------------------------------------------------------
# 黑区TLS配置
#-------------------------------------------------------------------------------

# 是否配置TLS: 0=否, 1=是
BLACK_ENABLE_TLS="1"

# 证书验证: 0=不校验, 1=校验
BLACK_TLS_VERIFY_CERT="1"

# TLS密码/证书编号
BLACK_TLS_PASSWORD="758850966"

# P12证书文件路径
BLACK_TLS_P12_FILE="/home/hy_media_server/bin/758850966.p12"

# TLS连接目标IP (留空则默认使用通用配置 MAIN_IP)
BLACK_TLS_CONNECT_IP=""

# TLS连接端口 (默认: 6661)
BLACK_TLS_CONNECT_PORT="6661"

# RTSP TLS监听地址 (使用通用配置 MAIN_IP)
BLACK_TLS_RTSP_LISTEN_IP=""

# RTSP TLS监听端口 (默认: 8554)
BLACK_TLS_RTSP_LISTEN_PORT="8554"

# RTSP TLS连接目标地址 (使用通用配置 MAIN_IP)
BLACK_TLS_RTSP_CONNECT_IP=""

# RTSP TCP端口 (默认: 1554)
BLACK_TLS_RTSP_TCP_PORT="1554"

# RTSP TLS端口 (默认: 8554)
BLACK_TLS_RTSP_TLS_PORT="8554"

#===============================================================================
#                              红区配置 (RED ZONE)
#===============================================================================

[RED]

# 红区节点ID
RED_NODE_ID="21"

# 红区本地IP地址
RED_LOCAL_IP="192.168.56.43"

# 红区NAT IP地址
RED_NAT_IP="192.168.56.43"

# 红区网关地址
RED_NAT_IP2="192.168.16.143"

# 红区加密类型: -1=未加密, 4=红区加密
RED_ENCRYPT_TYPE="4"

#-------------------------------------------------------------------------------
# 红区加密卡配置 (FPGA)
#-------------------------------------------------------------------------------

# 红区加密卡代理IP
RED_FPGA_AGENT_IP="192.168.55.30"

# 协商端口 (默认: 16001)
RED_FPGA_NEGOTIATION_PORT="16001"

# 节点端口 (默认: 16002)
RED_FPGA_NODE_PORT="16002"

# 数据端口 (默认: 16003)
RED_FPGA_DATA_PORT="16003"

# 联系端口 (默认: 500)
RED_FPGA_CONTACT_PORT="500"

#-------------------------------------------------------------------------------
# 红区Nginx配置
#-------------------------------------------------------------------------------

# Nginx proxy_pass 目标IP
RED_NGINX_PROXY_IP="192.168.16.254"

#===============================================================================
#                              安装包路径配置
#===============================================================================

# SIE安装包路径 (留空则使用默认 /home/deploy_sie_loongarch64_sqlite_UMP_V200R006B07.run)
SIE_PACKAGE=""

# SQLite安装包路径 (留空则使用默认 /home/deploy_sqlite_loongarch64.run)
SQLITE_PACKAGE=""

# Nginx安装包路径 (留空则使用默认 /home/deploy_nginx_loongarch64.run)
NGINX_PACKAGE=""
